{
  "name": "ggjs",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "nodemon": "^3.1.0"
  },
  "scripts": {
    "avvia": "concurrently \"nodemon backend/server.js\" \"cd frontend && npm start\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
